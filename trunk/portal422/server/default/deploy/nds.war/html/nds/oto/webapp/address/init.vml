##收货地址管理，如果没有VIP此页面不可以访问 $fromvipid就是wx_vip_id(会员id)
#if("$!fromvipid"=="")
	#set($url="/html/nds/oto/webapp/error/index.vml")
	$myweb.forwardTourl($url,$request,$response);
#end

## type的类型 pageAddress收货地址管理，不涉及订单管理； pagePay表示订单收货地址管理
#set ($type = $getutil.getString($request.getParameter("type"),"pageAddress"))

##获取收货地址
#set ($addressList =$myweb.getList("WX_ADDRESS","WX_SETAD","WX_ADDRESS.WX_VIP_ID=$!fromvipid","/@ID@",1))